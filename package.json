{
  "name": "btp-cap-rag-admin-hub",
  "version": "0.1.0",
  "description": "CAP backend proxy for SAP AI Core RAG services – admin and chat capabilities.",
  "dependencies": {
    "@sap-cloud-sdk/connectivity": "^4.4.0",
    "@sap-cloud-sdk/http-client": "^4.4.0",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "start": "cds-serve",
    "start:run": "cds run",
    "watch": "cds watch",
    "lint": "cds compile srv db >/dev/null",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  },
  "cds": {
    "requires": {
      "auth": "dummy"
    }
  },
  "private": true
}
